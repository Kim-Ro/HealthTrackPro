import { Typography } from "@mui/material";

function SettingsPage() {
    return <div>
        <Typography variant="h4" component="h1" mb={2}>Settings</Typography>
        <Typography variant="body1" component="p">This is the settings page.</Typography>
    </div>
}

export default SettingsPage;